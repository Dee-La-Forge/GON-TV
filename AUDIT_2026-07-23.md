# Audit 7 (nocturne) + passe simplify — 2026-07-23

Périmètre : les 8 commits post-revue (IndexedDB profond + fusion, apiCool unifié, ancres v69,
alertes R1-R3, anti-trou, deepenSec pacing). 4 auditeurs parallèles (2 correctness, 2 simplify),
auto-réfutation obligatoire, vérification sur pièce avant application. **18 correctifs appliqués**,
tous sûrs.

## Correctness — persistance (6)

1. **HAUTE — re-check `seq` après l'`await` disque de `load()`** : un load supplanté pendant la
   lecture IDB écrivait `oldestDone/fi/lm/la` du MAUVAIS contexte par-dessus le suivant
   (corruption possible des curseurs aggTrades en TF secondes). Une ligne, à l'endroit exact où
   le chemin secondes se protégeait déjà.
2. Repli localStorage de l'échec `idbPut` **mort par construction** (relisait les globaux au
   callback, le contexte avait déjà changé) → écrit désormais CE snapshot (borné BARS_CAP).
3. `bars.idx` en lecture-modification-écriture sur DEUX transactions → updates perdus entre
   onglets/apps (même origin = même DB) → orphelins `bars` jamais purgés. → `idbRmw` : get+put
   dans UNE transaction readwrite (IDB sérialise les écrivains).
4. Un trou d'exchange RÉEL (maintenance Binance > 3 pas) jetait TOUT l'enregistrement → cache
   neutralisé ~2 semaines. → `sanitizePersisted` : on garde le SUFFIXE après le dernier trou
   (atStart/fi neutralisés) ; secondes et mensuel restent au régime strict.
5. Fusion avec préfixe vide : `fi/atStart` de l'IDB décrivaient un `b[0]` disparu → `return ls`.
6. `idbOpen` sans timeout (hang navigateur pathologique = app blanche) → `Promise.race` 1,5 s,
   repli localStorage.

## Correctness — ancres & alertes (4)

7. **Alertes rétroactives au réveil TF secondes** : le juge d'âge R2 (queue du lot) était battu
   par un lot illiquide à queue FRAÎCHE (quelqu'un vient de trader après 8 h de veille) ; le
   latch `pollSlept` détenait l'info et était jeté sans servir dans la branche secondes. →
   `stale = pollSlept || (clockSynced && …)` — couvre aussi la bascule WS→poll (pollLast=0) et
   l'affamement de `clockSynced` pendant un ban partagé.
8. **Cache d'ancres à deux niveaux** : l'exigence large (±45 min couverte) rendait les TF
   secondes et toute mort < 1 h JAMAIS cacheables → recalcul étagé par frame. Un résultat
   d'étage 1 (fenêtre exacte) est stable dès que SA fenêtre est couverte ; l'exigence large ne
   s'applique qu'aux étages 2-4. + éviction PARTIELLE du cache (le clear() total en vue
   dézoomée >2000 POI forçait le recalcul intégral chaque frame).
9. Étage zone-PURE intercalé avant l'enveloppe cluster (fenêtre exacte) : l'enveloppe en
   premier pouvait attraper une bougie AVANT la vraie touche de zone (régression v67 évitée).
10. Seed whale-sonar : tué par le ban partagé d'un AUTRE module sans jamais être retenté
    (P99.9 sous-calibré des heures sur les alts) → re-planifié à l'échéance (idempotent via
    `ready`).

## Simplify (8, tous vérifiés sans changement de sémantique sauf n°15 — strictement plus sûr)

11. `loadPersistedDeep` : lecture IDB lancée PENDANT le parse localStorage (latences
    additionnées sur le chemin critique de chaque chargement).
12. `step = tfDef().sec` (identité prouvée sur les 23 TF — démasque la relation couture
    2*step ≤ tolérance 3*step).
13. `persistable()` : le concept « contexte persistable » défini une fois.
14. Étage 4 de la cascade supprimé : l'IIFE bougie-contenante était identiquement égale à
    `snapped` depuis le passage FLOOR (4 lignes mortes + un commentaire mensonger).
15. Reset ATOMIQUE de l'état d'alertes (`alertBarT/seenHi/seenLo` avec `alertPrev`) au
    changement de contexte et au rattrapage — l'invariant reposait accidentellement sur
    `alertPrev` seul.
16. Garde de `checkAlerts` : terme fantôme `price !== alertPrev` retiré (couvert prouvé par
    `hi/lo`, invariant lo ≤ price ≤ hi).
17. `W15 = 900` : source unique des bornes de fenêtre d'anchorSec (les littéraux 900/1800/
    2700/3600 masquaient le couplage recherche↔cache) ; grille legacy de snapToBarSec alignée
    FLOOR (le ceil résiduel re-créerait le +1 bougie si jamais atteint) ; hygiène branche
    naissance (e/zone/seek descendus dans la branche mort qui les utilise) ; clé bootcache IDB
    définie une fois ; `saveBootCache`/`loadBootCache` aplatis (le seam résout toujours — un
    seul catch ceinture).
18. Bump : poi-render v70, poi-feature v44, whale-sonar v30.

## Vérifié sain (auto-réfutations abouties — ne pas re-chasser)

Clone structuré synchrone du `put` (bar de tête mutable sans risque) ; step 2D/2W corrects ;
garde recouvrement en mensuel (dégénère proprement, tout l'historique 1M tient sous BARS_CAP) ;
bootcache : garde post-await + `rec` figé avant l'async ; migration LS pré-IDB couverte par le
GC 3 j du pagehide ; fp hors fenêtre → reset fenêtre pleine (unités vérifiées) ; CEIL→FLOOR
toléré par tous les usages ; alertes en replay structurellement muettes à la sortie ;
lOf/tOf O(1) : grille fact garantie par le pipeline, schéma/maxAge barrent les caches d'une
autre ère ; Math.max kline fact==1 ne fige jamais de mèche fantôme durable.
